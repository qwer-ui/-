/*新增用户信息*/
import {request} from '../../request'
export  const  PutUser=(url,{id,name,nick_name,password,email,mobile,dept_id,role_id})=>{
  return request({
    url,
    method:'put',
    params:{
      id,name,nick_name,password,email,mobile,dept_id,role_id
    }
  })
}
